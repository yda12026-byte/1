# 工作日志｜2026-09-26

## 已完成

- 扶摇密钥写入本地 `.env`，使用 `600519.SH` 验证行情、历史日 K、三张财报、财务指标和估值访问；七类探测均返回有效数据。
- 安装官方 iFinD `ifind-finance-data` Skill 1.4.0，配置用户授权；股票工具列表自检通过，并验证股票资料与新闻检索能实际返回内容。
- 增加不含密钥和原始数据的可复查探测脚本与访问审计记录。
- 根据用户补充的 Codex MCP 配置，将 9 个 iFinD 相关 Streamable HTTP 服务注册到本机 Codex 用户配置；配置解析和全部 9 个服务的初始化、工具列表请求通过。
- 用户确定天齐锂业（002466.SZ）为唯一主标的；扶摇七类数据接口对该股复查通过，iFinD 股票资料确认主营业务与一级行业。选择理由见 `docs/decisions/0003-target-stock.md`。
- 整理天齐锂业七个诊断维度的可勾选字段表；核对扶摇实际返回字段，并测试 iFinD 财务、股东、事件、风险、公告、行业及锂价指标。将缺少原文、历史序列或口径验证的项目标为待核，交用户筛选。
- 用户在原字段表左列用 `2` 标出 21 项不需要的字段；保留原表和用户标记，另生成 51 项的筛选后候选表 `docs/field-candidates-002466-v2.md`。
- 复核原表标 `2` 的全部 21 项，在筛选后表附上可用性及后台依赖：自由现金流近似值仍需购建长期资产支付现金，质押说明仍需累计质押比例；分业务数据、减值字段和三类锂价序列已补测，项目相关公告目前仍需原文核准。
- 用户随后决定采用筛选前的完整候选范围，只去掉暂未开放外部接入的能力。当前候选表恢复为 72 条，组合项“异动原因 / 主力资金”只保留已验证可访问的个股异动原因；原始表的旧 `2` 标记保留作历史记录。
- 在仓库根目录增加 `AGENTS.md`，记录新会话接手顺序、题目交付、当前 72 条字段范围、数据与密钥边界、验证和 Git 操作约定；README 增加入口。
- 补充 `AGENTS.md` 的文档维护清单，明确题目原文、README、TODO、worklog、ARCHITECTURE、decisions、字段表、数据契约、访问审计、AI 使用记录和测试说明各自的更新触发条件与写法。
- 用户指定除 HTML 前端外能用 Python 就用 Python；已将此偏好写入 `AGENTS.md`、架构草案、README 和决策 0004。检查时工作区已有未提交 `.mjs` 原型，未覆盖或纳入本次改动。
- 用户进一步要求已有脚本也尽量改成 Python。核对原 Node 脚本后，改写为 `scripts/probe_fuyao.py` 和 `scripts/probe_ifind.py`，两者真实接口复测分别为 7/7 类成功和 10/10 个服务成功；旧 `.mjs` 探测脚本已移除，Git 历史保留。
- Python 核心后端第一条“利润与经营现金流”切片已实现：统一对象、`cannot_say` 禁区、同一期年报数据、`Decimal` 计算、DeepSeek 受限分类/翻译及失败回退。8 项构造测试通过，真实调用已验证；Web、其余维度与部署未完成。
- 排查出 Python 默认代理连接拒绝，针对核准的官方 HTTPS 域名改用直连；期末时间按上海时区转换，避免 UTC 日期提前一天。
- 根据用户的新架构方向，将首条诊断从提问时调用扶摇改为独立预下载。新增 `download_data.py` 和本地快照存储：标准化两项年报字段、内容摘要 ID、临时文件原子替换、失败保留旧版、七天后标记过期。诊断只读快照并记录版本。10 项测试通过，真实下载和快照诊断通过；iFinD 产品字段、服务器数据库及刷新调度尚待实现。测试曾因系统临时目录写入权限失败，改在 Git 忽略的工作区缓存目录测试并复核。
- 用户进一步确认这是一次性考试产品，改为仅一次下载所需数据并使用固定快照。决策 0006 取代 0005 的七天过期、刷新调度和服务器数据库计划。首条两字段切片已改为文件存在时拒绝覆盖、读取时不因时间流逝失效；诊断把快照标为 `fixed`，`cannot_say` 增加 `NO_LIVE_DATA`，禁止称实时或最新。下载脚本在文件已存在时直接返回原快照 ID；诊断可通过 `DIAGNOSIS_SNAPSHOT_PATH` 读取部署用私有文件。10 项构造测试通过；清空金融数据密钥后，已验证重复下载不访问金融接口，诊断仍能读取同一快照并通过文案校验。当前快照仍仅有两项扶摇字段，不能充当完整产品数据集。
- 用户确定 2025-08-31 至 2026-08-31 为统一观察区间，以 2026-08-31 为数据截止日。按要求先实验下载 2026 年 8 月各类数据：扶摇 7 组、iFinD 17 组和深交所半年报 PDF 均已取到，原始文件在 Git 忽略目录。扶摇两股日线各 21 条、三张财报各 8 期；四类锂价各 21 行。发现 iFinD 历史估值含周末行、事件表混有窗口外日期、公告结果无原文 URL；财务单季/累计及单位仍需核准。详情见 `docs/experiments/2026-08-one-month-data-audit.md`，尚未制作最终一年快照。
- 将 72 条候选项生成 `config/diagnosis_fields.json`，增加 Python 固定意图与七维路由，记录来源、候选状态、计算输入及产品实现状态。多维问题取维度并集；仅利润与经营现金流专项可执行，其他路由不读取快照。新增命令行检查入口、配置一致性与异常路由测试；本地 16 项测试和 `git diff --check` 通过，决策见 0008。完整一年快照和 Web 仍待完成。
- 在同一固定两字段快照上增加净利润正负、经营活动现金流净额正负及现金流/净利润比值三个窄意图，并保留原方向关系意图。单项问题只引用本字段；缺失、超截止报告期和非正分母分别返回未知或不适用。对同比、指定年份、绝对差额等问题保留 `planned`，复合问题保留全部请求维度，避免以单期方向冒充趋势或漏掉估值/行业。新增 `scripts/diagnose_question.py` 入口；23 项构造测试通过，本地现有快照的三个新问法只检查意图、结论类型与证据数，不输出受限值。决策见 0009。
- 按用户要求生成 `docs/question-routing-map.md`：先列四类可执行窄问题，再列七维规划路由与全面/多维问题，最后逐条列出 72 字段的来源、候选状态和计算依赖。生成脚本由字段配置导出，支持 `--check` 防止文档漂移；补充历史估值分位、同行中位数、相对行业表现等隐含计算/对齐条件。
- 用户指出 72 字段等量展开不适合天齐锂业。核对公司年报与半年报的矿端、锂化工、价格和成本传导描述后，新增公司专属优先级配置：72 项逐一分为核心/支持/背景/弱化，研发费用默认弱化；单维、多维、全面诊断分别限制默认展示为 5/8/12 项。完整字段仍可钻取，用户点名弱化项可提升；高优先级数据缺口不拿次要字段替代。问题对照表同步显示优先展示字段、完整候选和逐项理由；27 项测试通过，Web 与完整数据仍未实现。
- 根据用户补充，把四档优先级写入四类已实现财务问题的每条证据和结论；比值计算证据保留输入证据优先级来源，受限 LLM 翻译接收优先级用于解读详略。优先级不改变公式和证据状态。新增伪造档位、断裂计算来源及 LLM 输入的构造测试；完整一年快照、其余维度计算编排与 Web 仍待实现。
- 用户确定购买 CloudBase 个人版后的部署路径：云托管从 Git 构建代码，已核准产品快照放云存储，运行时读取。核对 CloudBase 官方 Git 部署、对象存储挂载及安全规则文档；记录决策 0012 和部署清单，保持云上构建、存储权限、公开 URL 为待实测。未上传原始或发布快照，也未改动正在进行的数据核准工作。
- 按用户要求启动并完成一年原始数据采集：扶摇 19 组（18 组业务成功，除复权事件 `code=3002` 无匹配记录）、iFinD 128 组（两项连接错误重试后完成）及五份官方定期报告原文；Git 忽略目录保存请求与 SHA-256，不覆盖已有成功文件。两股各 242 条前复权日线。日期审计发现估值/行业各 366 个自然日行、其中各 105 个周末行，且 iFinD 公司资料文本包含截止日之后的日期。72 候选项中 70 项有原始来源文件待核、1 项无匹配事件、1 项异动历史接口不可回溯；未发布完整产品快照。新增采集、日期和字段覆盖脚本；把公告原文候选从“当前未取得”修订为“部分可用”并同步配置和问题表。测试命令 `py -m unittest discover -s tests -p 'test_*.py' -q` 为 33 项通过；详见 `docs/experiments/2025-08-to-2026-08-full-download-audit.md`。
- 第二会话接手一年数据核准：重新读取 AGENTS、字段表、数据契约、访问审计、工作日志和采集审计，先输出“已核准 / 待核 / 需要用户决定”三类清单，未生成完整快照。独立复核补充：扶摇 `report_date_ms` 是“该期次在最新一期同类报告中的出具日”而非披露日；iFinD `business_2026_h1`/`financial_2025_fy` 分业务字段实为“去年年报”口径、`financial_2026_h1` 同次返回季度标签不一致；公告检索无 URL、新闻有 URL、事件查询结果被截断并含窗口外日期；风险波动率为周频普通收益率、最大回撤不复权；以扶摇 242 交易日为基准量出各序列自然日/周末/休市工作日差异。用户确认口径：1b 累计值标注、2a 负基期绝对基期并注明、3a 统一按实际交易日过滤、4 EDB 锂价作行业环境证据、5 同行组为赣锋+中矿资源+永兴材料、6 估值 TTM/MRQ 先确认再展示、8 `code=3002` 显示“请求范围无匹配事件”、9 `f048` 保留为历史数据缺口；7 公告/新闻呈现方式暂缓。决定写入 `docs/decisions/0013-annual-data-approval-decisions.md`，并同步数据契约、字段表、采集审计。33 项测试、路由文档检查与 `git diff --check` 通过。

- 按决策 0013 补下载正式同行组：扩展 `scripts/download_annual_raw.py`，新增中矿资源 `002738.SZ`、永兴材料 `002756.SZ`（赣锋沿用已有 `peer`）；扶摇取得两股各 242 条前复权日线、各 12 期三张报表、各 5 期财务指标，iFinD 取得两家 2026-06-30 财务和四只股票 2026-08-31 同日估值（`ifind_peer_valuation_cutoff_group.json`）。更新 `scripts/audit_annual_coverage.py` 把同行组文件纳入 `f053`/`f054`/`f040`；覆盖审计仍为 70/1/1。原始文件仍在 Git 忽略目录，同行财务报告期语义与同日口径尚未核准。33 项测试、路由文档检查与 `git diff --check` 通过。

- 核实 iFinD 估值 TTM/MRQ 口径（决策 0014，前述 TODO 项已完成）：用元数据、扶摇累计报表反推和一次同查询对比，确认分子为总市值、`PE(TTM)` 分母为归母净利润 TTM（滚动 4 季、基准日=报表公告日期）、`PE(MRQ)` 为最新一期归母净利润×年化系数（2026 半年报验证为 ×2）、`PB(MRQ)` 分母为归母净资产、`PS/PCF(TTM)` 用收入/经营现金流 TTM；亏损期返回负 PE。发现分子绝对水平与本地归母 TTM 仍约差 5%、候选查询元数据出现“总市值 交易日期=最新”，列为未解释项；历史分位只在 iFinD 自身序列内计算。结论写入 `docs/decisions/0014-ifind-valuation-definitions.md` 并同步数据契约、字段表与 TODO。验证命令 33 项测试、路由文档检查与 `git diff --check` 通过。仅输出比值和结论，未把受限数值写入仓库。

- 补验 iFinD 估值口径（决策 0014）：对多个报告期反推 `PE(MRQ)` 年化系数，全部命中——一季报 ×4、半年报 ×2、三季报 ×4/3、年报 ×1；用 `fitz` 从官方 A 股半年报取归母净资产（≈合并总权益×0.8132），与 PB 反推一致，确认 `PB(MRQ)` 分母为归母净资产且 iFinD 可直接提供该字段；对分子做日期与分母交叉核验（市值跟随请求日期、2026 半年报归母净利润 iFinD=扶摇=官方 PDF 完全一致、PE/PB 与官方归母净资产互相一致），把约 5% 差异定位到“市值的绝对基准”而非日期或分母，列为未解释项。结论更新入决策 0014、数据契约、字段表与 TODO。33 项测试、路由文档检查与 `git diff --check` 通过。
- 接手 Codex 的“可操作对话页面与 Python 接口”计划：发现 Flask 页面、接口与 Dockerfile 原型已在 `a75ff94` 提交，但本机未装 Flask、从未运行且无 Web 测试。安装依赖后实测全部状态分支和浏览器钻取，随后修正：可执行意图跳过 LLM 分类、LLM 超时 20→12 秒（用户批准，响应由 10 秒以上降至约 3.6 秒）；提示词禁止在正文复述免责句（用户批准，四类文案由全部回退变为 4/4 通过，校验未放宽）；前端改为北京时间、中文输入证据名、去掉来源反引号。新增 `tests/test_webapp.py` 12 项，全套 45 项通过；决策 0015 记录框架、状态语义、追问与 LLM 分工。Docker/Gunicorn 本机不可用，容器构建与 CloudBase 部署待测。
- 复查 opencode 完成的同行组下载与决策 0014：同行文件内容有效、`PE(MRQ)` 系数复算成立。用 2 条只读 iFinD 查询查明原“约 5% 未解释”——PE/PB 市值是总股本（A+H）× A 股收盘价（比值 1.0000），独立“总市值”对 H 股按港股价计；同行组 PE 元数据“最新”标签不可靠但数值确为 8 月 31 日；iFinD 标“第一季度”的单季度归母净利润/营收经扶摇相减核对实为第二季度。更新决策 0013/0014、数据契约、字段表、访问审计、采集审计、TODO 与 AGENTS。另更正：本会话早前报的“北京时间 03:16”是 Git Bash 时区误读（实为 UTC），实际北京时间约 11:20。
- 用户选定“核心四维”与“公告/新闻仅作待核线索”。新增产品快照生成器（schema 2，142 个原始文件、242 个交易日，已发布到 Git 忽略的 `data/cache/`）和四维确定性计算：财务趋势 3 条、估值 3 条、行情 3 条、行业 3 条结论，每条可钻取公式与输入。核实扶摇 `total_debt` 为负债合计，不称净现金；2026-05 行业窗口加权不一致，相对行业表现标冲突。路由新增 `partial`；Web 返回 `runs`/`gaps`/`clues`，前端显示关键数字、中性评价与线索。真实 LLM 下全面诊断 12/12 文案通过校验。57 项测试通过。决策 0016；README、架构、数据契约、字段表、部署清单、测试说明、AI 记录、TODO 与 AGENTS 已同步。

- 按用户反馈调整页面：证据卡片折叠行直接显示名称、数值、状态；展开后主区只放数值、时间、口径、异常原因和计算方法/输入，来源、接口/字段、查询定位、快照下载、单位换算、公式编号与优先级收入“技术信息”二级展开。引入状态（有效/冲突/缺失/不适用）、评价、优先级和四个维度的分色；去掉 `RESEARCH DESK / 01`、`EVIDENCE CHAT` 英文眉题。浏览器核验：技术信息默认折叠、冲突证据琥珀色并前置原因、四维强调色生效、无横向滚动。

- 按用户第二轮页面反馈：维度回答前新增“总体解读”（LLM 须逐字包含各维度锚点、校验失败回退程序汇总，与分维度改写并行）；数据卡片去掉红绿，方向改用中性色＋符号；四类窄问题收入“更多示例”。真实 LLM 下全面诊断、估值、锂价同行三类总体解读均通过校验，并指出“财务与同行指标领先而股价表现落后”的分歧。新增总体解读测试，58 项通过。

- 结论、缺口与线索卡片均可单独收起（收起后保留标签与一行摘要，点标题栏或按钮切换），总体解读下提供“全部收起/全部展开”；点击关键数字或输入证据时自动展开所在卡片。浏览器核验：全面诊断 14 张卡片全部收起后对话区高度约由 10400px 降至 1770px；修正消息区横向溢出，手机宽度无横向滚动。

- 部署准备：`/healthz` 增加两份快照状态与 LLM 是否配置（不含路径）；向用户列出控制台步骤（上传两份快照到私有云存储、Git 构建云托管服务、端口 8080、挂载 `/mnt/snapshots`、四个环境变量、最小实例 1）。用户确认数据可公开展示、不加限流（决策 0017）。

- 复核 opencode 补数（5 月行业总市值加权、官方半年报 54 项摘录与 iFinD 交叉核对），接入经营质量与风险两维，并以有息债务计算净现金、补存货与扣非同比；相对行业表现恢复可计算。修正“经营现金流”误选经营质量的路由。重新生成产品快照（145 个原始文件）；本地真实 LLM 下全面诊断六维 20 条文案全部通过校验。60 项测试通过。决策 0018。CloudBase 服务已从 Git 构建上线（`/healthz` 正常，快照待挂载）。

- 按用户要求：买卖/预测/收益承诺类问题改为“说明涉及类别 + 模型能力范围 + 推荐问题”，无法识别的问题同样给推荐；锂价走势不再误选行情维度；窄财务问题注明 2025 年年报口径（决策 0019）。63 项测试通过，浏览器核验推荐问题可点击并得到全面诊断。安全检查通过；新增公网验收脚本；README 按评审视角重写；TODO 与各决策状态同步。

- 云部署：服务已从 Git 构建上线（端口 8080，环境变量已配），快照存储挂载两次失败。第一次选“腾讯云对象存储”，cosfs 报平台生成的 COS 地址缺少协议；第二次改选“云开发云存储”，同样失败，日志未取得。线上仍是无挂载的首个版本，诊断返回快照不可用。按用户安排交接给 Codex 继续处理云部署，现状、日志要点与可选路径写入部署清单的“状态与交接”一节。

- 13:36 Codex 接手 CloudBase：部署版本 `005` 的完整日志表明构建成功，`side-dns-cache` 的 `/mount.sh` 因 cosfs URL 缺少协议失败；把挂载对象目录改成无结尾斜杠后，试验版本 `008` 最终创建失败。平台表单没有 COS URL 编辑项。线上无挂载版本 `/healthz` 为 200，但两份快照 `missing`，21 项公网验收尚不能执行。
- 核对 COS 发现现有对象在 `snapshots/` 目录，原交接配置的 `snapshots/002466/` 路径不符；现有产品快照上传时间 12:25、控制台显示大小 297.32KB，本地新快照 ID `f15e78c2f368f02b87e7`、322118 字节，尚未重新上传。对产品对象做匿名 HEAD 得到 403。需核对目标对象名并替换后复查 ID 和私有权限。已取消仍在创建中的试验版本 `008` 发布，线上仍保持无挂载版本。
- 复核 `008` 部署详情并纠正此前归因：镜像构建和推送成功，事件只到 `check_eks_virtual_service : process, DescribeVersion_empty`，页面显示创建失败且无实例；日志没有 `cos.ap-shanghai.myqcloud.com` 或 cosfs 报错。`008` 曾被取消发布，现有记录不足以判断其创建失败根因，不能等同于 `005` 的 URL 格式错误。用户明确继续挂载、不采用启动下载；API Explorer 的只读查询触发扫码验证，用户暂不扫码，未调用 API。
- 用户完成腾讯云账号身份验证后，仅调用只读 API：`DescribeCloudRunServerDetail` 确认当前服务配置的 COS 挂载 Endpoint 为 `cos.ap-shanghai.myqcloud.com`（缺协议），源目录为 `/snapshots/002466`、目标目录为 `/mnt/snapshots`、`ReadOnly=false`；未输出连接密钥引用的值。`DescribeServerManageTask` 查询 `008` 返回 `Status=stopped`、`FailReason` 为空，`CreateVersion` 步骤仍为 `running`。因此 `008` 的失败记录对应停止任务，不是已证实的第二次 cosfs 报错。尚未更改配置或发起新部署。


- 云部署打通：核查 Codex 未提交的排查记录后，查腾讯云 tcbr API 文档确认 `VolumeConf.Endpoint` 可写入带协议地址，指导用户经 API Explorer 修正 Endpoint、源目录与最小副本（010 `https`、011 `http`），cosfs 仍失败且无原因。经用户批准启用决策 0020（启动时从私有 COS 下载快照）：单独分支实现并以伪造客户端测试（68 项通过），用无效密钥实测真实桶返回 `InvalidAccessKeyId` 证明链路可达；合并 `main` 后用户在控制台关闭挂载、配置只读密钥环境变量。17:05 线上 `snapshot_source=cos_download/ok`，产品快照 `f15e78c2…`，公网验收 21/21。Codex 的排查文档改动已按原样代为提交。提醒用户更换曾在对话中贴出的 DeepSeek 密钥。
- 在 `web-visual` 分支（不触发 CloudBase 自动部署，避免干扰 Codex 排查部署）完成页面视觉改进：重写样式表为统一深蓝灰色板（去除残留绿色）；多维回答在总体解读下增加维度概览条（每维评价符号与主结论，点击跳到分区）；等待时显示分步说明与已用时间（不伪造进度）。实测六维总体解读约一半因超过 320 字回退，改为上限 200 + 40×维度数并写入提示，回退文本每维只取主结论；复测 6 次全部通过校验。63 项测试通过。待部署稳定后由用户决定合并到 main。

## 下一步

- 补下载三家同行的同日估值、同区间前复权行情、同报告期财务，并按决策 0013 固定同行组口径。
- 按决策 0013 核实 iFinD 估值 TTM/MRQ 口径后再决定是否展示；公告/新闻呈现方式待用户决定前只作待核线索。
- 视时间接入经营质量、风险维度，或补 2026-05 行业窗口总市值加权数据。
- 在 CloudBase 实测 Git 构建、Gunicorn 启动与快照挂载，取得公开 URL。
- 逐字段确认单位、统计口径与数据时点，开始产品数据适配和界面主链路。
- 按最终实施字段范围一次性准备完整快照；部署时从服务器私有文件读取，并展示报告期和快照下载时间。

## 数据取得与摘录（iFinD / 官方 PDF 交叉核对）

本次只做数据取得与摘录，**未改动** `src/`、`scripts/`、`tests/`；所有输出写入 Git 忽略的 `data/raw/annual-2025-08-31_2026-08-31/` 新文件，未覆盖既有文件，也未提交 `data/`。文档不记录任何数值。

- Task A：iFinD 取出申万有色金属板块 2026-05 成份区间涨跌幅（总市值加权平均），另存 `ifind_sector_2026_05_capweighted.json`；表头确认为“成份区间涨跌幅(总市值加权平均)”，与已有的算术平均文件并存、互不覆盖。
- Task B：用 `fitz` 从 `official_stock_2026_h1.pdf`、`official_stock_2025_h1.pdf` 按半年累计/期末口径摘录：分产品（锂矿、锂化合物及衍生品）的营业收入、营业成本、毛利率；投资收益及对联营、合营企业的投资收益；扣非归母净利润；存货；有息债务（短期借款、一年内到期的非流动负债、长期借款、应付债券、租赁负债）；资产减值损失与信用减值损失；存货跌价准备期末余额；购建固定资产、无形资产和其他长期资产支付的现金；锂精矿产量；主要在建项目名称与工程进度（原文短句）。输出 `official_extract_h1.json`（数组，含 PDF 页码与表名）。
- Task C：用 `get_stock_financials` 查询两期各六项指标，保存 `ifind_crosscheck_h1_2026.json`、`ifind_crosscheck_h1_2025.json`；与 PDF 逐项比较写入 `official_extract_crosscheck.json`。结果：2026 期六项一致；2025 期五项一致、一项双方均无。
- 未取得项：锂化合物及衍生品的产量与销量、锂精矿销量（两期报告均未披露，记 `value=null` 并注明）；2025 半年报“应付债券”行未列示金额、iFinD 亦未返回该指标；临时公告原文与分部盈亏仍未取得。

## 线上视觉验收后的布局修正

- 用户重新上线后，公网验收脚本 `py scripts/smoke_public.py` 21/21 通过；浏览器检查公网页面时发现两个问题：桌面端整页与消息区各自滚动（消息区被 `max-height: calc(100vh - 380px)` 限高）；提问后 `scrollIntoView` 带动整页跳动约 200px。
- 修正：宽度 >900px 且高度 ≥560px 时整体锁定一屏，侧栏与消息区各自滚动，消息区去掉限高；提问后只滚动消息区（窄屏仍整页滚动并留出顶栏高度）。
- 新增输入区“收起/展开”按钮：收起后隐藏示例问题与提示，输入框变为单行；偏好仅存本浏览器 localStorage（读写包 try/catch）。本地 1440×800 实测：输入区高度约 200px → 72px，消息区 400px → 525px，页面高度等于视口；375px 手机宽度无横向滚动。
- `py -m unittest discover -s tests` 69 项通过。改动需重新部署后才能在公网看到。

## 优先级依据改为通俗表述

- 用户反馈“优先级依据”文字难懂（例：f045“回撤与波动率补充市场风险，按同一观察窗口的前复权日线计算”）。按“先说指标衡量什么、再说对天齐锂业为什么重要、最后写必要限制”的写法重写 `config/priority_profile_002466.json` 全部 72 条依据；四档优先级本身未改。去掉“首屏”“钻取”“决策 0013”等内部用语，保留分母非正不适用、近似值、市场报价不等于公司售价、公告须以原文为准等限制。
- 配置版本升为 `002466-priority-v2`（`src/diagnosis/priority.py`、`tests/test_diagnosis.py` 同步）；重新生成 `docs/question-routing-map.md`。
- 验证：`py -m unittest discover -s tests` 69 项通过；本地 `/api/chat` 全面诊断 20 条结论均返回 v2 新依据。

## 结论卡片底部说明合并

- 用户要求去掉卡片中部的“固定快照 · 下载于 … · 受限模型文案已校验”，并与底部“限制：”合并。`web/static/app.js` 改为卡片最下方一个说明块：第一行“限制：…”，把下载时间并入“仅代表固定快照（数据截止 2026-08-31，下载于 … 北京时间），不代表实时状态”一条（没有该条时自动补上）；第二行说明文字由受限模型生成且已校验，或为程序生成并附未通过校验的原因。
- 验证：本地“股价表现和波动”问题的 3 张卡片中部不再出现快照行，底部合并文字正确；69 项测试通过。

## 输入证据蓝链点击无反应

- 用户反馈计算框“输入证据（点击定位）”蓝链点击没反应。排查：全面诊断共 98 个输入证据链接，其中 58 个指向的证据（如分业务毛利、债务明细、估值序列）没有被任何结论直接引用、页面未渲染，`jumpTo` 找不到目标即静默返回；其余能跳转但无视觉提示。
- 修正（`web/static/app.js`、`app.css`）：目标不存在时在该计算框下方就地展开这条输入证据再定位；定位后该证据行淡蓝高亮约 1.6 秒（减少动效时改为描边）。
- 验证：本地全面诊断逐个点击 100 个输入链接，缺失目标全部就地渲染（55 条，部分链接指向同一证据）；手动点击“营业收入（2025-06 累计）”可展开并高亮；69 项测试通过。

## 重新部署后公网复核

- 用户部署提交 `9199f96` 后，`py scripts/smoke_public.py` 21/21 通过；浏览器在公网确认一屏布局、输入区收起、卡片底部说明合并、输入证据定位（100 个链接，55 条就地展开）及优先级依据 v2 均已生效。

## 重要事件维度接入巨潮资讯官方公告（决策 0022）

- 用户确认：采用巨潮资讯为公告权威来源；八类事项为定期报告、业绩预告、项目或扩产、诉讼仲裁、担保、回购、套期保值、利润分配；原文摘录采用程序抽取加人工抽查；行情图标出事件日期。
- 数据：巨潮清单 207 条（HTTP 200、无重复）；规则分类后八类共 25 份、其他 182 份；下载 21 份非定期报告 PDF（约 2.8MB）。程序摘录 21 条、业绩预告解析 3 条、失败 0。调试中修正三处抽取问题：泰利森进展与火情公告误取背景句、回购公告带出章节标题、业绩预告整句过长（改为引用类型与区间原文）。
- 结论：定期报告 4 份均在法定期限内披露（早 1–33 天）；三次业绩预告的实际归母净利润均落在区间内（位置 50.89%、58.68%、99.43%；2025 年度实际值与利润分配公告所载 462,625,452.36 元一致）。
- 代码：`src/diagnosis/events.py`、`dimension_events.py`、`product_snapshot._events`、`webapp.py`（事件状态、新闻线索、`price_chart`、旧快照回退）、前端摘录与行情图；优先级配置升为 v3（f056 依据）。新增 `tests/test_events.py`，全套 77 项通过。
- 本地产品快照已重建为 `8a115ce1361f68305b74`（旧文件改名为 `product_snapshot_002466.pre-events.json` 保留）。本地浏览器确认事件维度“可诊断”、7 条结论的模型文案均通过校验、行情图 19 个事件标记、摘录可打开原文页。
- 待办：人工抽查 9 条样本 → 重建快照 → 上传云存储覆盖 `snapshots/product_snapshot_002466.json` → 重新部署并公网复核。

## 抽查结果与“资本运作”类

- 用户对照 PDF 核对 9 条抽查样本：全部一致，已用 `record_event_spotcheck.py` 记录。
- 按用户要求新增第 9 类“资本运作”：标题规则匹配新 H 股配售及可转债发行、择机处置参股公司股权、全资子公司对外投资增资（公告与进展公告）、与专业投资机构共同投资合伙企业，共 5 份；债务融资工具注册与付息、H 股增发一般性授权未计入。补下 5 份 PDF，调整关键词两轮（共同投资公告误取第 11 页套话、增资进展公告误取背景句）。重新抽取后原 21 条摘录与 3 个业绩预告区间逐字不变；首版抽取文件保留为 `cninfo_event_extract.v1.json`。
- 事件维度增至 8 条结论（新增“资本运作”，并注明“择机处置”只是董事会授权）；77 项测试通过；本地快照重建为 `27fbf623d17077fc3e22`。资本运作类补抽 1 条待用户核对。
- 用户核对资本运作补抽样本一致，10 条抽查全部一致；产品快照重建为 `5f4fc986bc6ea4113368`（7 条摘录与 3 个业绩预告区间标“人工抽查一致”，其余 19 条为未抽中）。待上传云存储并重新部署。
- 用户上传快照并重新部署后，公网验收 21/21 通过，事件维度在线可诊断；修正状态标签写死“六维”的问题（本地已验证，待推送）。

## 图表：股价走势与同行（计划第三张，与事件行情图合并）

- 18:35 开工，18:38 完成代码、测试与本地浏览器验证（以本机时钟为准）。`price_chart` 改为天齐与三家同行期初 = 100 的前复权走势（附收盘原值），天齐最大回撤区间与行情维度共用 `max_drawdown()`（从 `market_run` 抽出），九类事件标记保留在天齐线上；前端同行灰色细线、天齐强调色粗线、线尾直接标名称并防重叠，淡紫阴影标回撤，悬停显示四家指数与收盘价，图例与说明注明来源和“历史走势不预示未来”。
- 验证：77 项测试通过（新增指数首末值与回撤区间断言）；本地浏览器确认 4 条线、回撤 -48.24%、24 个事件标记、悬停提示正确；375px 宽度下图宽 343px、无横向溢出（该宽度截图工具空白，未取得截图）。

## 图表：锂价走势（计划第二张）

- 18:41 开工，18:42 完成（以本机时钟为准）。行业维度新增 `lithium_series:{key}` 序列证据（作为“锂价均上涨”结论的背景证据，记录剔除的非交易日行数）；接口在涉及行业维度时返回 `lithium_chart`（四类锂价期初 = 100，附原值）；前端铜色深浅加线型区分，锂精矿最深，线尾直接标名称，悬停显示指数与原价，说明写明“锂价不等于公司实现售价、历史走势不预示未来”。
- 验证：新增锂价图测试（首末指数、序列证据存在）与接口测试，78 项通过；本地浏览器确认 4 条线、期末指数锂精矿 262／碳酸锂期货 213／碳酸锂现货 202／氢氧化锂 190、悬停数值正确。

## 图表：估值历史位置（计划第一张）

- 18:44 开工。接口在涉及估值维度时返回 `valuation_chart`：PB 在上、PE(TTM) 在下共用时间轴，33.3%/66.7% 分位对应倍数的虚线（与估值分位证据同一算法），截止日圆点与分位，半年报披露日（取事件块，2026-08-28）处标注 TTM 分母切换。
- 发现问题：PE(TTM) 序列 242 个交易日中 136 个为负值（亏损期），现有“市盈率 TTM 近一年分位 56.6%”证据把负值一并排序，33.3% 分位边界落在 -42.79，负倍数被当成“低”。图表对含非正值的面板不画分位区间、截止标签不写分位，并注明原因；现有 PE 分位证据的口径是否改为“不适用”或只在正值内计算，待用户决定（主结论用 PB，未受影响）。
- 验证：新增估值图测试（边界与分位证据一致、含负值时不给边界、切换日取自事件块）；79 项通过；本地浏览器确认两个面板、PB 截止 1.77 分位 22.7%（低区间）、边界 1.95/2.20。
- 用户选择把 PE(TTM) 近一年分位标为“不适用”：序列含非正值时分位证据状态为 `not_applicable`，理由写明负值交易日数（136 个，决策 0014 补记）；图表同一规则。顺带修正老问题：有效的分位证据此前也带着“截止日倍数不适用”的理由字段（页面不显示，但数据不对），现只在不可用时附理由。79 项测试（含新增断言）通过；真实快照复核：PB 分位 22.7% 有效、主结论“市净率位于自身近一年序列的低区间”不变，PE 分位为不适用。

## 对照题目自查与补缺

- 逐条对照题目原文：主链路、七维、分工、钻取、合规、可追溯、仓库公开、AI 记录与测试说明均已具备；发现两处缺口。
- 缺口 1：事件维度接入后七维全部可运行，全面诊断不再出现“尚未接入的维度”卡片，“未知”证据与“待验证问题”在主链路中消失（统计：正面 8、负面 2、矛盾 1、中性 17、未知 0）。修正：风险维度新增两条 `unknown` 结论——锂价对利润的敏感性（f070）、海外项目/政策/汇率敞口（f071），各挂一条 `missing` 证据并写明原因。全面诊断增至 30 条结论，新增测试通过（80 项）；本地验收 21/21，两条未知结论的模型文案通过校验。
- 缺口 2：README 过时——测试数、决策数、上线状态、三张图与事件维度、架构图、环境变量（缺 `DIAGNOSIS_COS_*` 等），且没有单独的“未做事项”。已全部更新并新增“未做事项”一节。
- 待办：用户重新部署后跑公网验收并浏览器复核三张图与未知卡片。

## 提交材料文档一致性复核

- 本次重新核对题目原文、干净的 `main` 工作树及线上 API：本地 80 项单元/接口测试通过；`py scripts/smoke_public.py` 公网 21/21，七维可诊断、事件摘录有原文链接，在线产品快照 ID 为 `5f4fc986bc6ea4113368`。匿名 Git 读取确认远端 `main` 与本地 `f265364` 一致。
- 修订 README、AGENTS、架构、数据契约、部署清单、TODO、测试说明、AI 使用记录和 `.env.example` 的现状描述：七维已上线；公告为巨潮原文证据，新闻仍为线索；CloudBase 使用私有 COS 启动下载，早期 cosfs 挂载错误标为历史记录。锂价利润敏感性与海外敞口保持“未知”，股权质押与解禁仍非正式证据。
- 发现自动生成的问题路由表仍写事件 `planned`，且净现金、现金覆盖率的候选配方沿用扶摇 `total_debt`（实为负债合计）。同步修订候选表、路由目录生成脚本和导出文案，重建 `config/diagnosis_fields.json` 与 `docs/question-routing-map.md`；这些只是路由元数据与说明，产品数值公式未变。
- 未改题目原文、历史决策、金融数据或诊断计算程序。可选演示视频仍未录制。
- 验证：路由目录维持 72 条候选项，问题对照表 `--check` 通过；全套 80 项测试通过，12 个受影响文档相对链接无断链，`git diff --check` 通过，新增差异未匹配常见密钥或私钥格式。

## AI 使用记录开头总述

- 按用户审定文案，在 `docs/ai-use-and-validation.md` 的逐项表格前增加总述：分别注明 Codex、Claude Code、opencode 的模型与参与环节；说明确定性计算和受限 LLM 的分工；写明候选人的范围取舍、数据与口径核验、Web 视觉效果审阅修改、CloudBase 部署操作和具体纠错。原有逐项记录保持不变。

## 必交材料署名

- 用户已在 README 和 AI 使用记录前加入姓名、学校；核对题目提交清单后，在可独立查看的测试说明标题下补同一署名。题目原文保持只读，内部架构和决策记录不重复署名；保留用户对 README 与 AI 使用记录的排版修改。
