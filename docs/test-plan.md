# 测试说明

状态：Python 核心切片与本地 Web 主链路已测试；公开部署待测。

问题路由另有配置一致性测试：72 项候选字段的名称、维度、来源和状态须与当前字段表逐行一致；专项只展开已实现字段；其他问题可查看计划字段但不读取快照、不生成结论；无效 LLM 意图回退固定规则。测试结果见下文。

| 场景 | 预期 | 结果 |
| --- | --- | --- |
| 正常主链路 | 提问、诊断、证据钻取可完成 | 2026-09-26 本地通过（接口测试 + 浏览器）；公开 URL 待测 |
| 数据缺失 | 显示未知及缺失原因，不补造数字 | 2026-09-26 通过：缺失/冲突证据值为空、结论未知；快照缺失返回 503 |
| 接口失败 | 显示失败状态，保留可用证据 | 快照校验失败返回 503；LLM 超时回退程序文案并标注 `llm_unavailable`（2026-09-26 通过）|
| 数据冲突或固定快照时点 | 标注冲突、报告期和下载时间，不称实时/最新 | 固定快照、禁区文案与冲突均已测；页面显示北京时间下载时刻 |
| 极端值或零分母 | 计算安全、单位与口径正确 | 零/负净利润分母经 Web 接口返回不适用（2026-09-26 通过）|
| 合规边界提问 | 不输出确定性预测、收益承诺或直接买卖建议 | 2026-09-26 通过：涨跌/买入问题返回 `unsupported_question` |
| 公开部署 | URL 可访问，核心操作可用 | 待测 |
| CloudBase 快照读取 | Git 构建的云托管实例从私有云存储挂载读取同一摘要版本，缺文件或校验失败明确报错；浏览器不可直接读取快照 | 待测 |
| DeepSeek LLM 连接 | 模型可用且最小文本调用成功 | 2026-09-25 已通过；详见决策 0002 |
| 扶摇数据权限 | 行情、历史 K 线、财报、财务指标、估值返回有效数据 | 2026-09-25 已通过；详见数据访问审计 |
| iFinD 数据权限 | 工具列表可获取，股票与新闻实际调用有内容 | 2026-09-26 已通过；详见数据访问审计 |

## Python 核心切片验证（2026-09-26）

原先在线取数切片执行 `py -m unittest discover -s tests -p 'test_*.py' -v`：8 项通过。覆盖同口径背离及比值输入追溯、报告期/口径不一致、缺失、零分母、接口错误、禁区文案拒绝及回退、引用与数字校验、非支持问题不调用数据源、上海时区期末转换。随后按决策 0005 改为预下载；在线取数结果仅为迁移前历史验证。

预下载版本曾执行同一命令：10 项通过，当时含七天过期测试；这是决策 0005 的历史结果。`py scripts/download_data.py` 曾真实发布两项扶摇年报字段；随后 `py scripts/diagnose_profit_cash.py` 只读快照，受限意图分类和 DeepSeek 文案校验通过。本地快照位于 Git 忽略的 `data/cache/`，受限数值没有写入仓库。

按决策 0006 改为固定快照后，同一命令再次执行：10 项通过。覆盖已存在文件拒绝覆盖、读取旧下载时间快照仍保持 `fixed`、快照 ID 回传及“实时/最新”禁区。清空金融数据密钥后运行 `py scripts/download_data.py` 返回 `already_prepared` 和原快照 ID；`py scripts/diagnose_profit_cash.py` 继续只读该快照，文案校验通过。这验证了当前两字段切片的运行路径，不代表其余字段或 Web 已完成。

迁移后的 `py scripts/probe_fuyao.py 002466.SZ` 七类请求均 `ok`；`py scripts/probe_ifind.py` 十个服务均 `ok`。这只验证当前凭据和接口结构，不等于 Web 主链路、全部 72 条候选字段或公开部署已完成。

## 固定截止日的月度实验（2026-09-26）

以 2026-08-01 至 2026-08-31 为样本窗口运行 `py scripts/experiment_august_data.py --source fuyao`、`--source ifind` 和 `--source official`。扶摇 7 组与 iFinD 17 组均得到 HTTP 200 和有内容的业务响应，深交所半年报 PDF 下载并核对文件头、大小与 SHA-256；原始文件被 Git 忽略。逐类记录日期和行数后，识别出 iFinD 估值含周末日期、事件包含窗口外日期、公告检索缺原文 URL。测试只证明实验接口覆盖，不证明完整一年快照、字段口径或 Web 已通过。详见[实验下载审计](experiments/2026-08-one-month-data-audit.md)。

## 问题路由与字段配置（2026-09-26）

执行 `py -m unittest discover -s tests -p 'test_*.py' -v`，覆盖旧切片与新路由：逐行核对 72 条候选表、七维映射和状态计数；验证利润/现金流专项只选 4 条相关候选，估值与行业的多维问题展开 15 条，全面诊断展开 72 条；计划中或投资建议问题不读取快照，错误的 LLM 归类不能把估值问题送入已实现的专项。重建配置命令为 `py scripts/build_route_catalog.py`，重建后须审阅 Git 差异。路由测试不证明其余维度已可执行。

本次在本地执行结果：16 项通过；`git diff --check` 通过。公开 Web 主链路、完整快照和其他维度的数值计算仍待测。

## 扩大的固定快照财务问法（2026-09-26）

执行同一测试命令，23 项通过。新增场景验证净利润与经营现金流单项问题仅引用自身来源，另一项出错不阻止单项回答；来源缺失或报告期晚于 2026-08-31 时结论为未知；比值计算可追溯两项输入，净利润非正时不适用；同比、指定年份、绝对差额和买卖问题不会误走窄意图；复合问题不会漏掉估值、行业等维度。本地现有固定快照还对三个新问法核对了意图、结论类型与证据数量，不输出具体金融数值。结果只证明两字段快照支持这四类受限问法，不代表整个财务趋势维度或其他六维完成。

`docs/question-routing-map.md` 从同一字段配置生成，运行 `py scripts/export_question_routing_md.py --check` 检查它与当前配置一致。此检查验证文档同步，不验证待接入字段的真实数据权限或数值口径。

## 天齐锂业优先级与有限展示（2026-09-26）

新增检查：公司专属配置须覆盖全部 72 字段；全面诊断默认 12 项且锂价先于弱化字段，经营质量默认前五项包含量价成本而不含研发费用；用户明确问研发费用时该字段进入首位；估值问题先展示 PB 与历史估值；四个已实现窄意图不因展示上限失去必需输入。完整 72 项继续留在钻取候选。尚未实现 Web，展示计划不等于实际页面效果或数据就绪。

执行 `py -m unittest discover -s tests -p 'test_*.py' -v`：27 项通过；`py scripts/export_question_routing_md.py --check` 与 `git diff --check` 通过。风险问题中锂价敏感性字段虽列为核心，仍保持 `待核` 和 `pending`，只作为未来需呈现的证据缺口。

## 一年原始下载准备（2026-09-26）

`py -m unittest discover -s tests -p 'test_*.py' -q`：33 项通过（含同日其他工作新增的优先级测试）。本次新增测试检查按月拆分的请求窗口连续覆盖 2025-08-31 至 2026-08-31、72 条候选项都有来源获取计划、iFinD MCP 工具错误不会被解析为有效数据。真实下载完成 19 组扶摇（含一组业务无匹配记录）、128 组 iFinD 和 5 份 PDF；重跑不会覆盖已有成功文件。`py scripts/audit_annual_coverage.py` 输出 70 项有来源文件待核、1 项无匹配事件、1 项历史接口不可用；`py scripts/check_annual_dates.py` 发现估值和行业各 105 个周末行，并检出 EDB 首日空桶。详情见[一年原始数据审计](experiments/2025-08-to-2026-08-full-download-audit.md)。当前验证仅针对采集和文件覆盖；逐字段单位、报告期、原文定位、发布快照、Web 结果和公开部署仍待测。

## 优先级进入证据链（2026-09-26）

构造测试覆盖来源、计算证据和结论的字段 ID、档位、理由及配置版本；比值计算输入的优先级来源按证据 ID 对齐，公式结果保持原值；伪造档位、错配字段或删掉计算来源应被运行对象校验拒绝。模拟 LLM 调用检查请求包含优先级、状态和方向，不含财务原值。`py -m unittest discover -s tests -v`：33 项通过；`py scripts/export_question_routing_md.py --check` 与 `git diff --check` 通过。真实在线 LLM 的篇幅遵循情况及 Web 展示仍待验证。

## 对话页面与 Python 接口（2026-09-26）

环境：Windows 本机 Python 3.13，`py -m pip install -r requirements.txt` 后 `py webapp.py`；Gunicorn 与 Docker 本机不可用，容器构建未实测。

`tests/test_webapp.py` 新增 12 项，使用 `data/cache/` 下临时构造快照：健康检查与 bootstrap 快照时点；四个示例均返回可追溯运行对象（结论引用存在的证据、计算输入可解析、证据带下载时间与优先级）；有限追问可继承、“它们”需承接两项指标、无上下文或伪造上下文要求补全、“为什么/继续”要求补全；估值、全面诊断、同比在快照缺失时仍只返回字段缺口；买卖与涨跌问题拒答；快照缺失与篡改分别报 `missing`/`invalid` 并返回 503 且不泄露路径；冲突和缺失证据值为空、结论未知；零与负分母比值不适用；非 JSON、非对象、非字符串、空白、超 500 字返回 400，超大请求体返回 413；可执行问题不调用 LLM 分类、非可执行问题仍调用；LLM 翻译失败回退；各响应不含密钥名、授权头、快照路径或云存储域名。

`py -m unittest discover -s tests -p 'test_*.py'`：45 项通过。

浏览器验收（真实 DeepSeek、本地两字段快照）：点击“利润和经营现金流匹配吗”示例约 3.6 秒返回结论（调整前 10 秒以上），文案通过校验；展开计算证据可见公式、版本、中文输入证据名，点击输入名跳至来源证据；横幅和证据显示北京时间下载时刻与快照 ID。“估值高吗”仅显示字段缺口。375px 宽度无横向滚动。另用 DeepSeek 直连核对四类问题的受限文案，调整提示词后 4/4 通过校验（此前 4/4 因 `NO_LIVE_DATA` 回退）。

## 核心四维与完整产品快照（2026-09-26）

新增 `tests/test_dimensions.py` 10 项与 `tests/test_webapp.py` 2 项，使用 `tests/product_fixture.py` 构造的产品快照（可手算的合成数值，非市场数据）：四维运行对象均通过 `validate_run`，锚点无数字且包含于回退文案、计算证据有公式并可追溯输入；同比按绝对基期、负基期 300% 手算一致、与扶摇比率差 0.1 个百分点以上标冲突、低基期加注；零基期与非正净利润为不适用；“货币资金减负债合计”公式写明不是净现金；PB 分位 100、同行中位数 30、负 PE 不适用并使相关结论为未知；设计价格路径的区间收益 20%、最大回撤 −40%、量比 1.44；行业窗口含算术平均时复合收益为冲突、相对表现未知；同行排名不受 `names` 键顺序影响（回归：发布后 JSON 键排序曾使首位变为赣锋）；产品快照不可覆盖、篡改后摘要校验失败；LLM 多条改写逐条校验，禁区词、缺条目与超时均回退；2024 年问题保持 `planned`、“营收”问题为 `partial`。Web 测试覆盖四维与全面诊断的 `runs`/`gaps`/`clues`、事件只返回线索、产品快照缺失或篡改时四维 503 而窄问题可用、bootstrap 维度状态。原路由测试按新语义更新（估值等为 `implemented`，趋势问题走财务维度而非窄意图）。

`py -X utf8 -m unittest discover -s tests -p 'test_*.py'`：57 项通过；`py scripts/export_question_routing_md.py --check` 通过。

真实数据验证：`py scripts/build_product_snapshot.py` 发布快照（142 个原始文件、242 个交易日；估值剔除 124 行非交易日、碳酸锂/氢氧化锂各 19 行、锂精矿 6 行，与决策 0013 审计一致）。本地 Web + 真实 DeepSeek：“全面诊断”约 12–15 秒返回四维 12 条结论、事件线索（公告 106、新闻 126）与 6 项缺口，12 条模型文案全部通过校验；“财务趋势”3/3 通过；“近期公告”只返回线索；“2024 年净利润同比”为 `planned`；原四类窄问题仍正常。实测发现并修正：同行排名键序错误、多条改写 12 秒超时（改为 30 秒、并行）、三处锚点与回退文案不一致、锚点含禁词“最新”。页面无横向滚动。容器构建与云上部署仍未测。

页面层次调整（2026-09-26）：浏览器检查证据摘要行含名称/数值/状态，技术信息默认折叠且标记正确，冲突证据显示琥珀色原因框，四个维度分色，侧栏与标题区无英文眉题，页面宽度无横向滚动。样式与前端结构改动，后端测试不受影响（57 项通过）。

总体解读与配色（2026-09-26）：`test_overall_summary_is_validated_and_falls_back_to_program_anchors` 覆盖无 LLM 时的程序汇总（含各维度锚点、注明未接入维度、无数字），以及 LLM 文本含数字、含“买入/低估”、缺锚点时的回退与失败原因；Web 测试检查维度回答含 `summary` 且包含结论锚点。58 项通过。浏览器：首屏示例 6 个、“更多示例”折叠 4 个窄问题；总体解读位于卡片之前；答案区所有计算色值中无红绿色。真实 DeepSeek：全面诊断、估值、锂价与同行三问的总体解读均 `passed`。

卡片收起（2026-09-26，浏览器）：单卡收起后正文隐藏、按钮变“展开”并显示一行结论摘要，点标题栏可恢复；“全部收起”作用于本次回答全部 14 张卡片；消息区与 375px 宽度均无横向滚动。前端改动，后端 58 项测试不受影响。
