# 数据与证据约定

状态：待接口实测后细化。

每条关键证据至少记录：`source`（来源）、`as_of`（数据时点）、`field_or_text`（原始字段或原文定位）、`value`、`unit`、`scope`（统计口径）、`status`（有效/缺失/冲突/过期/失败）及必要的计算公式或查询参数。

每条诊断结论区分 `fact`（事实）、`inference`（推断）、`unknown`（未知），并归入正面、负面、矛盾或未知证据。界面需能从结论回到支撑材料。

待补：实际接口字段映射、更新频率、过期阈值、同行比较口径及数据使用限制。
